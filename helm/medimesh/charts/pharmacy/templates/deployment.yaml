apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.pharmacy.name }}
  namespace: {{ .Values.global.namespaces.backend }}
  labels:
    app: {{ .Values.pharmacy.name }}
    tier: backend
    app.kubernetes.io/part-of: medimesh
spec:
  replicas: {{ .Values.pharmacy.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.pharmacy.name }}
      tier: backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: {{ .Values.pharmacy.name }}
        tier: backend
    spec:
      initContainers:
        - name: wait-for-mongodb
          image: busybox:1.36
          command:
            - sh
            - -c
            - >
              until nc -z {{ .Values.global.mongo.host }}-0.{{ .Values.global.mongo.host }}.{{ .Values.global.namespaces.db }}.svc.cluster.local {{ .Values.global.mongo.port }};
              do echo 'Waiting for MongoDB...'; sleep 3; done
      containers:
        - name: {{ .Values.pharmacy.name }}
          image: "{{ .Values.pharmacy.image.repository }}:{{ .Values.pharmacy.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.pharmacy.containerPort }}
              name: http
          env:
            - name: PORT
              value: {{ .Values.pharmacy.containerPort | quote }}
            - name: MONGO_URI
              value: "mongodb://{{ .Values.global.mongo.host }}-0.{{ .Values.global.mongo.host }}.{{ .Values.global.namespaces.db }}.svc.cluster.local:{{ .Values.global.mongo.port }},{{ .Values.global.mongo.host }}-1.{{ .Values.global.mongo.host }}.{{ .Values.global.namespaces.db }}.svc.cluster.local:{{ .Values.global.mongo.port }},{{ .Values.global.mongo.host }}-2.{{ .Values.global.mongo.host }}.{{ .Values.global.namespaces.db }}.svc.cluster.local:{{ .Values.global.mongo.port }}/{{ .Values.pharmacy.dbName }}?replicaSet={{ .Values.global.mongo.replicaSet }}"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: medimesh-secrets
                  key: JWT_SECRET
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: medimesh-config
                  key: NODE_ENV
            {{- if .Values.pharmacy.extraEnv }}
            {{- toYaml .Values.pharmacy.extraEnv | nindent 12 }}
            {{- end }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.pharmacy.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.pharmacy.containerPort }}
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
          resources:
            requests:
              cpu: {{ .Values.pharmacy.resources.requests.cpu | quote }}
              memory: {{ .Values.pharmacy.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.pharmacy.resources.limits.cpu | quote }}
              memory: {{ .Values.pharmacy.resources.limits.memory | quote }}
