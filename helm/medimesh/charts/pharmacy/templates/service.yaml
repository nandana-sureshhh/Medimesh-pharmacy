apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.pharmacy.service.name }}
  namespace: {{ .Values.global.namespaces.backend }}
  labels:
    app: {{ .Values.pharmacy.name }}
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.pharmacy.name }}
    tier: backend
  ports:
    - port: {{ .Values.pharmacy.service.port }}
      targetPort: {{ .Values.pharmacy.containerPort }}
      protocol: TCP
      name: http
